pub fn main() {
    let x = 10;
    let y = 20;
    
    // Test comparison operators
    if x < y {
        let result = x + y;
    }
    
    // Test logical operators with boolean values
    let cond1 = true;
    let cond2 = false;
    if cond1 && cond2 {
        let combined = x * y;
    }
}